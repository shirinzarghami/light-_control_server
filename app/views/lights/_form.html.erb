<%= form_for([@location, @light]) do |f| %>
  <% if @light.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@light.errors.count, "error") %> prohibited this light from being saved:</h2>

      <ul>
      <% @light.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :on_code %><br>
    <%= f.text_area :on_code %>
  </div>
  <div class="field">
    <%= f.label :off_code %><br>
    <%= f.text_area :off_code %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
